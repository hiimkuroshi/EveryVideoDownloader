# Project working memory

Before making project changes, read `project-vault/MEMORY.md`, then open only the linked notes relevant to the task.

After a material implementation, decision, discovery, or verification:

- Update the relevant note in `project-vault/`.
- Append a short entry to `project-vault/08 - Work Log.md`.
- Update `project-vault/MEMORY.md` only when the index needs a new or changed pointer.
- Keep `MEMORY.md` below 200 lines and store details in topic notes.
- Never store passwords, cookies, tokens, personal paths, or other secrets in the vault.

